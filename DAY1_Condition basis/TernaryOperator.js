let num = 12;
let result = num > 0 ? "positive" : "negative";
console.log(result);

//! Q.2 Grade a student based on marks:

// ? marks >= 90 → "A"

// ? marks >= 75 → "B"

// ? marks >= 50 → "C"

//? else → "Fail"

let marks = 82;
let grade = marks >= 90 ? "A" : marks >= 75 ? "B" : marks >= 50 ? "c" : "Fail";
console.log(grade);

// !✅ 1. Check if a year is leap year or not using ternary
